# platform/values/hub/karpenter.yaml
#
# Hub-tier overlay, layered on top of platform/values/base/karpenter.yaml
# by argocd/hub/20-karpenter.yaml.
#
# clusterName/clusterEndpoint pulled from platform/values/hub/cilium.yaml
# (k8sServiceHost: "10.0.11.10", k8sServicePort: "6443") and the retired
# platform/values/hub/auto-scaler.yaml's autoDiscovery.clusterName
# (hub-dev-k8s) — same control-plane identity, different consumer.
settings:
  clusterName: hub-dev-k8s
  clusterEndpoint: "https://10.0.11.10:6443"